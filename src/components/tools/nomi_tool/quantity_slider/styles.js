const styles = (theme) => ({
	root: {
		marginBottom: theme.spacing(1) / 2
	},
	slider: {
		// maxWidth: "96%",
		// maxWidth: "60%",
		width: 384,
		marginLeft: theme.spacing(1)
	},
})
export default styles