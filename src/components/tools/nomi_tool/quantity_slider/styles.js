const styles = (theme) => ({
	root: {
	},
	sliderBox: {
		display: "flex",
		alignItems: "center",
	},
	slider: {
		width: 144,
		margin: `0 ${theme.spacing(1)}px 0 ${theme.spacing(2)}px`
	},
})
export default styles