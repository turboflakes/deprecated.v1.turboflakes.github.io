const styles = (theme) => ({
	root: {
		marginBottom: theme.spacing(1) / 2
	},
	sliderBox: {
		display: "flex",
		alignItems: "center",
	},
	slider: {
		width: 224,
		margin: `0 ${theme.spacing(1)}px 0 ${theme.spacing(2)}px`
	},
})
export default styles