const styles = (theme) => ({
  root: {
    position: "relative",
    backgroundColor: "rgba(241, 241, 240, 0.95)",
    borderBottom: `solid 2px #FFF`,
  },
  canvas: {
    padding: 0,
    margin: "0 auto",
    display: "block"
  }
})
export default styles