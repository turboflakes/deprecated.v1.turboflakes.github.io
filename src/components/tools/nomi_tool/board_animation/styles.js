const styles = (theme) => ({
  root: {
    position: "relative",
    // height: "90vh",
    backgroundColor: theme.palette.neutrals[100],
    borderBottom: `solid 2px #FFF`,
  },
  canvas: {
    padding: 0,
    margin: "0 auto",
    display: "block"
  }
})
export default styles