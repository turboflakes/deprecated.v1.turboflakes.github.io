const styles = (theme) => ({
  root: {
    padding: `0 0 ${theme.spacing(1)}px ${theme.spacing(6)}px`
  },
  searchForm: {
    display: "flex",
    alignItems: "center",
    width: 540,
  },
  searchIcon: {
    marginRight: theme.spacing(2)
  },
})
export default styles