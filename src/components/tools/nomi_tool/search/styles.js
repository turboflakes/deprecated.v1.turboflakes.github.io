const styles = (theme) => ({
  root: {
    
  },
  searchForm: {
    display: "flex",
    alignItems: "center",
    width: 700,
  },
  searchIcon: {
    marginRight: theme.spacing(2)
  },
})
export default styles