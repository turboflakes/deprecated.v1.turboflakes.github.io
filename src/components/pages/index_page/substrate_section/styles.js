const styles = (theme) => ({
  root: {
    height: 704,
    backgroundColor: "#0B1317",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    color: "#FFF",
  },
  container: {
    ...theme.container,
  },
})
export default styles