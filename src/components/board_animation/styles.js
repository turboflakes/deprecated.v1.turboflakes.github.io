const styles = (theme) => ({
  root: {
    position: "relative",
    height: "90vh",
    backgroundColor: theme.palette.neutrals[100]
  },
  canvas: {
    padding: 0,
    margin: "0 auto",
    marginBottom: "1rem",
    display: "block"
  }
})
export default styles